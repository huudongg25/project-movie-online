import Admin from "./admin.entity";
import Banner from "./banner.entity";
import Category from "./category.entity";
import DepositHistory from "./depositHistory.entity";
import FavoriteMovie from "./favoriteMovie.entity";
import Movie from "./movie.entity";
import Paid from "./paid.entity";
import Review from "./review.entity";
import User from "./user.entity";
import WatchHistory from "./watchHistory.entity";

const createTable = () => {
  // Category.sync().then(() => {
  //   console.log("Category create ok");
  // });
  // User.sync().then(() => {
  //   console.log("User create ok");
  // });
  // Movie.sync().then(() => {
  //   console.log("Movie create ok");
  // });
  // Review.sync().then(() => {
  //   console.log("Review create ok");
  // });
  // FavoriteMovie.sync().then(() => {
  //   console.log("FavoriteMovie create ok");
  // });
  // Paid.sync().then(() => {
  //   console.log("Paid create ok");
  // });
  // WatchHistory.sync().then(() => {
  //   console.log("WatchHistory create ok");
  // });
  // DepositHistory.sync().then(() => {
  //   console.log("DepositHistory create ok");
  // });
  // Banner.sync().then(() => {
  //   console.log("Banner create ok");
  // });
  // Admin.sync().then(() => {
  //   console.log("Admin create ok");
  // });
};
export default createTable;
